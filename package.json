{
  "name": "sim-base",
  "private": true,
  "version": "1.0.0-space",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "generate:video": "tsx scripts/generate-space-video-entry.ts",
    "generate:video:survival": "tsx scripts/generate-video-auto.ts",
    "generate:video:core": "tsx scripts/generate-video.ts"
  },
  "devDependencies": {
    "@napi-rs/canvas": "^0.1.80",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  }
}
